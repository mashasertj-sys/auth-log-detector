#Auth Log Anomaly Detector

Инструмент для обнаружения в логах аутентификации, написанный на Python.

```bash

# git clone https://github.com/mashasertj-sys/auth-log-detector.git
# cd auth-log-detector

# Запустить всё  командой
# docker-compose up

# Открыть в браузере
#  http://localhost:8080